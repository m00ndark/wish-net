# .NET build output
bin/
obj/
publish/
*.user

# Server-only secrets — never commit or deploy
api/config.php

# Local Claude settings (also covered by the global git ignore)
.claude/settings.local.json
